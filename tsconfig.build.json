{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "sourceMap": false,
    // 不要map
    "declaration": false,
    // 不要.d.ts
    "incremental": false
    // 不要 增量编译
  },
  "exclude": ["test", "dist", "**/*spec.ts", "logs", "public/uploads", "public/temp"]
}
